/**
 * PostCSS configuration for processing CSS files.
 *
 * This configuration includes Tailwind CSS and Autoprefixer plugins
 * to handle CSS transformations and vendor prefixing.
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
