import ButtonSend from './ButtonSend';

/**
 * Story for displaying an ideal ButtonSend component.
 */
export const Ideal = () => <ButtonSend onClick={() => {}} />;

/**
 * Story for displaying a loading ButtonSend component.
 */
export const Loading = () => <ButtonSend loading onClick={() => {}} />;

/**
 * Story for displaying a disabled ButtonSend component.
 */
export const Diabled = () => <ButtonSend disabled onClick={() => {}} />;
